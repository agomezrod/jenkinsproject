pipeline {
    agent {
        label 'jenkinslave'
    }
    stages {
        stage('testing 123') {
            steps {
                sh 'echo hello from master node'
            }
        }
    }
}
